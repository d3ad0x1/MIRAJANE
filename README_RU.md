<p align="center">
  <img src="assets/mira-banner.jpg" alt="Mira Docker Control Panel Banner" width="100%">
</p>

# 🧩 Mira — Панель управления Docker 
Неоновая, быстрая и современная UI-панель для управления контейнерами.

---

# 🇷🇺 Русский

## 🚀 О Mira
**Mira — это современная веб-панель для управления Docker-контейнерами, сетями, образами, томами и шаблонами.**  
Фронтенд построен на React + Vite + Tailwind, backend — FastAPI (Python).

Основные возможности:
- 🔥 Мониторинг контейнеров в реальном времени
- 🌐 Создание и удаление сетей
- 📦 Управление образами  
- 🧱 Управление томами  
- 📝 Быстрое создание контейнеров на основе шаблонов 
- ✨ Неоновый / киберпанк интерфейс

---

## 📸 Screenshots

### 🧱 Dashboard / Containers
<p align="center">
  <img src="assets/screens/containers.png" width="100%" alt="Containers page">
</p>

### 📦 Images
<p align="center">
  <img src="assets/screens/images.png" width="100%" alt="Docker Images page">
</p>

### 🌐 Networks
<p align="center">
  <img src="assets/screens/networks.png" width="100%" alt="Docker Networks">
</p>

### 📝 Templates
<p align="center">
  <img src="assets/screens/templates.png" width="100%" alt="Templates UI">
</p>

## 📦 Установка

### 1️⃣ Клонирование репозитория
```bash
git clone https://github.com/d3ad0x1/MIRAJANE.git
cd mirajane-mira-panel
```

### 2️⃣ Создать файл .env

```bash
cp .env.example .env
```

Заполнить нужные параметры:

```bash
API_URL=http://ТВОЙ_IP:ПОРТ/api/v1
```

### 3️⃣ Запуск через Docker

```bash
docker compose up -d --build
```

### 4️⃣ Запуск фронтенда локально

```bash
cd frontend
npm install
npm run dev
```

### 🛠 Требуемое ПО

- Docker + Docker Compose
- Node.js 18+
- Git

### 🧯 Устранение проблем

❗ API не отвечает или возвращает ошибку 500
- Проверить логи backend-контейнера:
```bash
docker compose logs api -f
```

❗ Белый экран на фронтенде

- Посмотреть логи dev-сервера:
```bash
npm run dev
```
- Возможно, неверно указан API_URL в .env.

## 🗂 Структура проекта

- frontend/ — React + Vite интерфейс Mira.
В dev → порт 5173, в продакшене работает за nginx.
- backend/ — FastAPI API для управления Docker.
Основной API-сервис, порт 8088 внутри контейнера.
- mira-data/ — постоянное хранилище Mira:
шаблоны, пресеты, пользовательские конфиги.
Рекомендуется включить в регулярные бэкапы.
- assets/ — баннеры, логотипы, изображения UI.
- docker-compose.yml — оркестрация всех сервисов Mira.
- README.md, README_RU.md, README_EN.md — документация проекта.

## 🔐 Безопасность

Mira управляет Docker-хостом, поэтому доступ к панели = доступ к серверу.

Рекомендуется:

- Не публиковать Mira напрямую в интернет.
- Использовать:
    - VPN (WireGuard / OpenVPN / ZeroTier);
    - внутреннюю сеть (LAN);
    - SSH-туннели при необходимости.
- Ставить панель за reverse-proxy:
    - Nginx / Traefik / Caddy;
    - HTTPS + базовая авторизация / токены / SSO.
- Осторожно работать с /var/run/docker.sock:
при его пробросе в контейнер любой доступ к панели = root над Docker.

📄 **License:** [MIT License](LICENSE)